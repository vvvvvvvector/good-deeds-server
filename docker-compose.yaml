version: '3.5'

services:
  main: 
    container_name: main
    build: 
      context: ./
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - 3000:3000
    command: npm run start:dev
    restart: always